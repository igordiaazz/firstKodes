export const TERMS_EN: Record<string, string> = {
  print:
    'Command that displays text or values on the screen. Everything shown goes inside parentheses.',
  string:
    'Data type that represents text. Always goes inside quotes, like "Hello" or "Python".',
  variable:
    'Named memory space that stores a value. The value can change throughout the code.',
  '=': 'Assignment operator. Puts the value on the right into the variable on the left.',
  '>=': 'Comparison operator: greater than or equal. Returns true if the left value is greater than or equal to the right one.',
  Numbers:
    'Numeric values (integers or decimals). Unlike strings, they don\'t use quotes.',
  if: 'Conditional structure that executes a block of code only if the condition is true.',
  else: 'Structure that catches the opposite case of if. Runs when the if condition is false.',
  elif:
    'Combination of else + if. Adds a new condition after an if, before the else.',
  while:
    'Loop structure that executes a block while the condition is true.',
  '-=': 'Subtraction assignment operator. Subtracts the right value from the variable and stores the result in it.',
  range:
    'Function that generates a sequence of numbers. range(5) generates 0, 1, 2, 3, 4.',
  break:
    'Command that immediately stops the current loop, regardless of the condition.',
  for: 'Loop structure that iterates through each item of a sequence (like a list or range).',
  def: 'Keyword that defines (creates) a new function in Python.',
  return:
    'Command that sends a value from the function back to where it was called.',
  list:
    'Structure that stores multiple ordered values inside square brackets []. It can contain numbers, strings, and other types.',
  index: 'Number that indicates the position of an element inside a list. The first position is 0.',
};
